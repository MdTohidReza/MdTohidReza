#include<stdio.h>
float int area(float)
float int perimeter(float)
int main()
{
	int len,p,a,area,peri;
	printf("enter the length");
	scanf("%d",&len);
	a=area(len);
	p=peri(len);
	printf("the area of square is =%f",a);
	printf("the perimeter of square is =%f",b);
	return 0;
}
float area(floatl)
{
	float a;
	a=l*l;
	return(a);
}
float perimrter (floatl)
{
	float p;
	p=4*l;
    return(p);
}
